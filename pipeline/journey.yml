platform: linux

image_resource:
  type: docker-image
  source:
    repository: rowanto/docker-java8-mvn-nodejs-npm
    tag: 'latest'

inputs:
- name: pet-source

#outputs:
#- name: pet-journey

run:
  path: sh
  args:
  - -exc
  - |
    whoami
    env
    mvn -version