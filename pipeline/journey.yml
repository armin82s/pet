platform: linux

image_resource:
  type: docker-image
  source:
    repository: rowanto/docker-java8-mvn-nodejs-npm
    tag: 'latest'

inputs:
- name: pet-source

#outputs:
#- name: pet-journey

run:
  path: sh
  args:
  - -exc
  - |
    cd pet-source/pet-server
    mvn spring-boot:run
    npm install -g bower gulp
    npm install
    bower install
    gulp protractor
