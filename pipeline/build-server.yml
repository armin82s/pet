platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: '3.3.9'

inputs:
- name: pet-source

outputs:
- name: built-pet-server
  path: pet-source/pet-server/target

run:
  path: mvn
  args: ['clean', 'install', '-X']
  dir: pet-source