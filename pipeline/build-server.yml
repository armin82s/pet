platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: '3.3.9'

inputs:
- name: pet-source

outputs:
- name: pet-server

run:
  path: mvn
  args: ['clean', 'install']
  dir: pet-source/pet-server